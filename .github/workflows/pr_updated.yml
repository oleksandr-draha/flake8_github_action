name: PR Updated

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

jobs:
  pr_updated:
    name: PR Updated
    runs-on: ubuntu-latest
    steps:
    - name: Show PR
      run: |
        echo "${{ github.event.action }}"
        
